# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# uv
.venv/
.python-version

# Docker Compose — el archivo de override local se ignora; el template
# vive en compose.override.yml.example (commiteado).
compose.override.yml

# Environment secrets — la URL real del AVL upstream (ORIGIN_AVL) nunca se
# committea. El template vive en .env.example. Ver spec bridge-gtfs-rt R-03.
.env
